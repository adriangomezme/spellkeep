import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function DecksScreen() {
  return (
    <ScreenPlaceholder
      title="Decks"
      icon="layers"
      subtitle="Build and manage your decks"
    />
  );
}
