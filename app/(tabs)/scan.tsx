import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function ScanScreen() {
  return (
    <ScreenPlaceholder
      title="Scan"
      icon="scan"
      subtitle="Scan cards with your camera"
    />
  );
}
